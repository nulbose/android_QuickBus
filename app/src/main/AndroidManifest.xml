<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickBus"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TimetableActivity" android:label="Timetable"/>
        <activity android:name=".SelectLocationActivity" android:label="SelectLocation"/>
        <activity android:name=".BookingCompleteActivity" android:label="BookingCompleteActivity"/>
        <activity android:name=".TicketInfoActivity" android:label="TicketInfo"/>
        <activity android:name=".UserInfoActivity" android:label="UserInfo"/>
        <activity android:name=".BusSelectionActivity" android:label="BusSelection"/>
        <activity android:name=".DateSelectionActivity" android:label="DateSelection"/>
        <activity android:name=".SeatSelectionActivity" android:label="SeatSelection"/>
        <activity android:name=".SelectLocationActivity1" android:label="SelectLocation1"/>
    </application>

</manifest>